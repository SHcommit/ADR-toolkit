# Examples

- [`quickstart.md`](quickstart.md) — INIT a repo, RECORD a decision with a
  machine-checkable constraint, then watch CHECK catch a real violation of
  it and clear once the code is fixed. Every command and its output in
  this walkthrough was actually run, not written from memory — copy any
  command and it will do exactly what's shown.

For a second, larger example, this repository dogfoods itself: see
[`../docs/decisions/`](../docs/decisions/) for the real ADRs recording the
architectural decisions made while building this toolkit.
