__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Adapter install-time symlinks. Every adapter README tells the user to
# create these inside this working tree; committing a real symlink breaks
# Windows checkouts without core.symlinks, which the CI matrix includes.
adapters/codex/skills/adr-toolkit
adapters/gemini-cli/skills/adr-toolkit
adapters/antigravity/skills/adr-toolkit

# Task-by-task implementation plans (superpowers subagent-driven-development
# artifacts) — kept locally for reference, not part of the public tree.
# docs/superpowers/specs/ (the design docs) stay tracked; only plans/ is
# excluded. History from before this commit still has them if ever needed.
docs/superpowers/plans/
